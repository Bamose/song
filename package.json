{
  "name": "song",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker:build": "docker compose build",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:up:announce": "docker compose --profile announce up -d && docker compose logs --no-color announce",
    "docker:urls": "docker compose logs --no-color announce"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.18.2",
  "engines": {
    "node": ">=20.18.1"
  },
  "devDependencies": {
    "turbo": "^2.5.8",
    "wrangler": "^4.43.0"
  }
}
